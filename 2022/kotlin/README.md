# Kotlin Advent of Code Solutions

To compile and run kotlin code use
```bash
kotlinc day1.kt -include-runtime -d day1.jar && java -jar day1.jar
```
`setting.gradle` needs to be present at the projects "root" for `kotlin_language_server` to index
the project
